package com.github.dylmeadows.eontimer.reference.timer;

import com.github.dylmeadows.eontimer.reference.Gen4TimerMode;

public interface Gen4TimerConstants {

    Gen4TimerMode DEFAULT_MODE = Gen4TimerMode.STANDARD;

    int DEFAULT_CALIBRATED_DELAY = 500;

    int DEFAULT_CALIBRATED_SECOND = 14;

    int DEFAULT_TARGET_DELAY = 600;

    int DEFAULT_TARGET_SECOND = 50;
}
