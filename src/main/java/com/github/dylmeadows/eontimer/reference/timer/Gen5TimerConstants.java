package com.github.dylmeadows.eontimer.reference.timer;

import com.github.dylmeadows.eontimer.reference.Gen5TimerMode;

public interface Gen5TimerConstants {

    Gen5TimerMode DEFAULT_MODE = Gen5TimerMode.STANDARD;

    int DEFAULT_CALIBRATION = -95;

    int DEFAULT_TARGET_DELAY = 1200;

    int DEFAULT_TARGET_SECOND = 50;

    int DEFAULT_ENTRALINK_CALIBRATION = 256;

    int DEFAULT_FRAME_CALIBRATION = 0;

    int DEFAULT_TARGET_ADVANCES = 100;

    double ENTRALINK_FRAME_RATE = 0.837148929;

}
