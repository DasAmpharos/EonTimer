// ─── Console ───
export enum Console {
  GBA = 'GBA',
  NDS_SLOT1 = 'NDS - Slot 1',
  NDS_SLOT2 = 'NDS - Slot 2',
  DSI = 'DSI',
  THREE_DS = '3DS',
  CUSTOM = 'Custom',
}

// ─── Gen5 ───
export enum Gen5Mode {
  STANDARD = 'Standard',
  C_GEAR = 'C-Gear',
  ENTRALINK = 'Entralink',
  ENTRALINK_PLUS = 'Entralink+',
}

// ─── Gen3 ───
export enum Gen3Mode {
  STANDARD = 'Standard',
  VARIABLE_TARGET = 'Variable Target',
}

// ─── Action ───
export enum ActionMode {
  AV = 'A/V',
  AUDIO = 'Audio',
  VISUAL = 'Visual',
}

export enum ActionSound {
  BEEP = 'Beep',
  DING = 'Ding',
  POP = 'Pop',
  TICK = 'Tick',
}

// ─── Custom Phase ───
export enum CustomUnit {
  MILLISECONDS = 'ms',
  ADVANCES = 'Advances',
  HEX = 'Seed (Hex)',
}
